import { DisabledControllDirective } from './disabled-controll.directive';

describe('DisabledControllDirective', () => {
  it('should create an instance', () => {
    const directive = new DisabledControllDirective();
    expect(directive).toBeTruthy();
  });
});
