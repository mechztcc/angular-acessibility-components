import { KeybordManagerDirective } from './keybord-manager.directive';

describe('KeybordManagerDirective', () => {
  it('should create an instance', () => {
    const directive = new KeybordManagerDirective();
    expect(directive).toBeTruthy();
  });
});
